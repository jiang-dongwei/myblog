## 人工验证记录 [HVR-S6-001]

**验证类型**: Xbox Series X|S 1914 BLE Profile与Windows驱动实机测试
**所属步骤**: S6-A / S6-B / S6-C
**验证时间**: 2026-08-13
**结论**: 静态与编译验证通过，等待实机

---

### 共同决策

```
1. ESP32-C6仅走BLE，不尝试Classic Bluetooth。
2. Xbox Profile改为Series X|S 1914的045E:0B13/0509和完整HID Report Map。
3. 不实现振动，不改变RP2350输入语义。
4. 不为描述符升级在普通开机自动清bond；Windows测试时人工删除旧设备并重新配对。
```

### AI验证记录

| 操作 | 结果 |
|------|------|
| 1914参考描述符逐字节对比 | PASS，reference=283，current=283 |
| Xbox PnP/序列号/Report特征主机测试 | PASS |
| Profile状态、UART协议、Gameplay Gate回归 | PASS |
| `git diff --check` | PASS（仅现有CRLF提示，无空白错误） |
| ESP32-C6 Ninja构建 | PASS |
| BIN | `build/fightpad12slim_c6_ble_hid.bin`，712288字节 |
| SHA256 | `CFE79A02FDDB8ED0CA209DB8C48909733209266920CAB6FB7B040107865FB11F` |

### S6-C实机验证目标

- [ ] Windows蓝牙设置中删除旧`FP12Slim-C6`设备及缓存后，按ESP32-C6 GPIO13重新配对。
- [ ] 启动日志显示`Xbox Series X|S BLE`、Report 1长度16、Map长度283。
- [ ] 设备管理器出现`Bluetooth LE XINPUT compatible input device`或等效XInput设备路径。
- [ ] `gamepadtesterr.com`显示Xbox且`mapping: standard`，不再是`0000 0000`。
- [ ] 全部按键、方向、LT/RT与RP2350 USB XInput语义一致；无振动符合设计。
- [ ] 断开PC蓝牙后无需GPIO13即可自动重连原绑定设备。
- [ ] 普通重启不清bond、不重新配对，仍保持Xbox Profile。

### 关键日志

```
active BLE Profile=1 (Xbox Series X|S BLE), report id=1 len=16 map=283 max_rpts=4
pair button debounced: pressed
pair button pressed: opening/restarting 30000 ms pairing window
BLE status -> pairing (0x03)
pairing flow: existing BLE link disconnect requested ...
pairing flow: BLE disconnect command consumed ...
advertising as FP12Slim-C6 (fast interval, pairing-open connections)
BLE encryption changed status=0
bonded peer captured for automatic reconnect
reconnect queued ...
```
