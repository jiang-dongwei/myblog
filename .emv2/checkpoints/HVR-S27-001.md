## 人工验证结果 [HVR-S27-001]

**验证类型**: Web Config 品牌实机页面检查
**所属步骤**: S27-D 设备 Web Config 桌面/移动端和亮/暗主题实页验证
**记录时间**: 2026-08-03

---

### 操作与预期

- 操作：进入设备 Web Config，检查导航栏、模式选项和首页欢迎标题。
- 预期：左上角显示 Fightpad Logo 与 `FIGHTPAD`，右上角与 `PS3` 同级的选项显示 `FIGHTPAD`，首页显示“欢迎使用 FIGHTPAD Web 配置工具！”。

### 【人类观察输入】

| 时间 | 观察内容 |
|------|----------|
| 2026-08-03 | 左上角仍显示 GP2040 Logo；右上角与 PS3 同级的选项仍显示 GP2040；首页仍显示旧的 GP2040 欢迎标题。 |
| 2026-08-03 | 返工版本已显示大号 `FIGHTPAD` 文字，但前置 SVG Logo 加载失败，页面出现破图图标和 `FIGHTPAD logo` 备用文字。 |
| 2026-08-03 | 删除导航图片并重新连接 Web Config 后，页面仍无变化；需核对 `www/src`、`www/build`、`fsdata.c` 与最终 UF2 是否属于同一轮产物。 |

### 【AI初步分析】

- `www/src` 中导航栏和中文首页标题已经改为 FIGHTPAD，但 `www/src/Data/Buttons.js` 的 GP2040 模式标签漏改。
- 设备使用的 Web 页面来自 `lib/httpd/fsdata.c`；当前 `www/build` 和 `fsdata.c` 均早于本轮品牌源码修改，仍包含旧 GP2040 文案与 Logo。
- Fightpad 推荐配置中的 `SKIP_WEBBUILD=TRUE` 会跳过 Web 前端构建，因此仅修改 `www/src` 后直接编译固件不会更新设备页面。
- 新截图确认文字字标已经生效；异常区域是 `<img>` 加载失败后的浏览器占位和 `alt` 文字，不是额外的品牌文字。
- 用户决定导航栏不再加载图片，只保留文字字标并清除图片占位与间距。
- 最新复测未生效，当前优先检查产物链路和时间戳，不继续猜测页面布局代码。
- `Navigation.jsx/.scss` 修改时间为 13:35，现有 Web JS/CSS 修改时间为 13:36。
- `lib/httpd/fsdata.c` 为 11:37:31，两个 httpd 对象文件为 11:37:46，最终 `build/GP2040-CE_0.0.0_Fightpad12Slim.uf2` 为 11:37:58。
- 因此 UF2 输出目录和 Web 指向均正确，但被烧录的 UF2 比本次删除图片的修改早约两小时，固件内没有本次修改。

### 【共同决策】

```
决策: 失败，进入返工
原因: 当前fsdata.c和UF2均生成于11:37，早于13:35至13:36的删除图片修改，设备仍运行旧内嵌页面
下一步: 完整执行www的npm run build以生成新哈希资源和fsdata.c，再编译build目录UF2、烧录并强制刷新浏览器
```

### 相关日志

- 无；本次证据为用户实际进入设备 Web Config 后的页面观察。
