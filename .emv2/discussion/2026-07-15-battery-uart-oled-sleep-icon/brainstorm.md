# 方案比较

1. 串口直接复用 BQ27220 的 2 秒采样节拍，不再维护 SOC 档位表和去重状态。
2. 输入侧在 Core0 捕获 GP30/31/32 原始边沿并发布最后活动时间；显示侧 Core1 只读取时间戳，避免重复接管 GPIO 和重复去抖。
3. OLED 使用已有 `setPower(false/true)` 做休眠与唤醒；休眠期间跳过清屏、绘制和 I2C 刷帧。
4. 主页面复用已有四格电池图标、`percentToBars()` 和百分比绘制函数，维持图标右上角、数字右下角；电压、电流和 FCC 仍从 Battery Info 菜单查看。
