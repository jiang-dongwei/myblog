# RGB Effect 菜单精简需求

## 需求

- Key Effect 删除 `Static Theme`。
- Key Effect 的 `Breathing` 进入颜色选择菜单，实现单色呼吸；颜色列表包含 `OFF`。
- Base Effect 删除自动变色 `Breathing`。
- Base Effect 的 `Breathing` 进入颜色选择菜单，实现单色呼吸；颜色列表包含 `OFF`。
- Base Effect 的 `Static Theme` 菜单名改为 `Rainbow`。

## 约束

- 不改变 GP22/GP40 引脚、PIO、LED 数量。
- 尽量保留底层旧 effect index 兼容已有保存配置。
