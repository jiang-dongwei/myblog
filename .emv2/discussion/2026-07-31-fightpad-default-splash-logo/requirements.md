# 需求确认

讨论ID：`2026-07-31-fightpad-default-splash-logo`

## 启动 Logo

- 目标板级配置：`Fightpad12Slim`。
- 图像来源：用户提供的 `C:\Users\15779\Downloads\zimo.TXT`。
- 图像格式：128×64 单色位图，逐行式，顺向高位在前，阴码。
- 数据大小：固定 1024 字节。
- 显示方式：保持 `SPLASH_MODE_STATIC`。
- 显示时间：保持 3000 ms。
- 图像位置和内容保持字模原样，不做居中平移或反色。

## 量产行为

- 新设备烧录后使用板级默认 Logo，不需要逐台进入 Web Config 上传。
- 保留 Web Config 的 Logo 配置能力；用户保存过的持久化启动图可以覆盖板级默认值。
- 已有配置的测试板需要恢复出厂或擦除配置后验证新的默认 Logo。
- 按项目约定不运行编译，由用户完成构建、烧录和实机验证。
