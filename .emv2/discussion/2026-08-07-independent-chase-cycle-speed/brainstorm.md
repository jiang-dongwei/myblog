# 方案推演

## 采用：按整圈周期计算归一化位置

用 `(elapsed % cycleMs) * ledCount / cycleMs` 直接得到当前头灯索引。这样1000ms和2000ms分别是完整一圈的真实周期，主循环偶尔延迟时只会跳到正确位置，不会累计变慢。

## 未采用

- 不使用83ms和105ms两个离散步进定时器，因为20ms刷新周期会把实际更新推迟到100ms和120ms附近。
- 不继续共享灯位步进；两条不同长度灯链无法在共享步进下同时满足1秒和2秒周期。
- 不拆分动态色轮，两条链仍保持相同颜色变化。
