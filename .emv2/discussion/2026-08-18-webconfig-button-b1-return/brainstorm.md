# Web Config Button预览B1直接返回 - 方案决策

仅把返回条件从松开边沿改成按下边沿还不够：旧页面仍可能在同一帧绘制B1动画，返回后的上一级也可能收到同一次松开。

最终方案：

- Button页在B1按下边沿请求返回。
- DisplayAddon在绘制旧页前立即完成页面切换。
- ConfigScreen在由按住状态进入时等待该次按钮释放，不把同一次松开识别为新动作。
- 修复EventManager注销回调循环的错误迭代器增量；这是实机退出卡死的根因。
