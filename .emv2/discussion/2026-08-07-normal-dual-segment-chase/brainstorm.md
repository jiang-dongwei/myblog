# 方案推演

## 采用：复用蓝牙双段几何

在普通 GP40 与 GP22 Chase 分支中分别计算两个 `chaseHeads`，第二个头为 `(head + count / 2) % count`，每个头向后绘制三灯拖尾。颜色继续来自普通效果共享色轮。

## 未采用

- 不直接调用蓝牙渲染函数，因为它只处理 GP40 且固定纯蓝、50ms。
- 不统一使用 19 灯索引再裁剪到 GP22，避免 12 灯链出现跳步和不对称。
- 不修改共享动画计时，避免破坏现有 100ms 同步节奏。
