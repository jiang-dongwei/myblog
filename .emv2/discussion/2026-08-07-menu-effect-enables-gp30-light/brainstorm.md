# 方案推演

## 采用方案

在现有三个非 OFF 选择分支中同步设置 `g_manualLightEffectsEnabled = true`，随后统一调用 `persistConfig()`。

该方案直接复用 GP30 的持久化状态，不引入第二个开关，也不改变 `All OFF`、蓝牙覆盖或低电保护。

## 未采用方案

- 不在渲染层根据效果反向推导 GP30 状态，避免每帧改写用户配置。
- 不让颜色 `OFF` 自动打开灯光，避免出现“状态为 ON 但颜色明确为 OFF”的歧义。
