# 硬件对齐

- GP22：12颗按键WS2812灯链，仅调整软件位置周期，不改PIO、灯数或亮度。
- GP40：19颗底灯WS2812灯链，本次不改周期。
- GP2～GP20：现有低有效游戏按键，复用已消抖`debouncedGpio`状态，不新增GPIO读取者。
- GP30/GP31/GP32：现有拨轮/按键活动时间戳发布路径保持不变。
- OLED：I2C0 GP0/GP1，继续通过显示驱动`setPower()`休眠和唤醒。
- 不新增中断、DMA、定时器或硬件连接。
