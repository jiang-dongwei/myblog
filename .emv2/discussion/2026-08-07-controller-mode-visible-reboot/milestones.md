# 实施里程碑

## S34-A 控制器模式重启黑屏（completed）

- 不同模式触发 RAM 黑屏标志。
- 普通灯效和蓝牙状态灯均不能覆盖黑帧。
- 相同模式保持无保存、无重启、无黑屏。

## S34-B 静态检查（completed）

- 检查标志不进入持久化配置。
- 检查黑屏发生在重启事件之前，且现有 watchdog 路径不变。

## S34-C 构建烧录与实机验证（pending）

- 由用户验证模式切换时约 500ms 的明显熄灯、USB 重枚举和启动后原灯效恢复。
