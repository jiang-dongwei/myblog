# GPIO13 Pairing Toggle RP2350 - 方案决策

采用统一发布点去重：当快照有效且新状态等于当前状态时直接返回。

该方案同时满足：

- 重复`0x03`不会生成新事件或重置Pairing动画。
- `0x03`后的任意不同状态都会获得新序号并立即替换覆盖层。
- OLED与GP40无需增加两套局部状态机，也不会出现页面不同步。
- 保留ESP32-C6重发机制带来的丢帧恢复能力。
