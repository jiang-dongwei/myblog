# GPIO13 Pairing Toggle RP2350 - 硬件与边界

- GPIO13物理配对按键属于ESP32-C6侧，RP2350不直接读取该引脚。
- 两芯片通过既有UART0固定8字节帧传递蓝牙状态。
- RP2350 Core0驱动GP40状态灯，Core1驱动OLED；二者读取同一个临界区保护的状态快照。
- 本轮仅修改RP2350工程，不修改ESP32-C6固件或硬件定义。
